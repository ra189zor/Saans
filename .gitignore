# node
node_modules
dist
dist-ssr
*.local
.vite

# python
__pycache__/
*.py[cod]
.venv/
venv/

# editors / os
.DS_Store
.claude
.vscode/

# secrets
.env
kaggle.json

# Source artwork for the question guides — 16.7 MB of PNGs. The app ships the
# WebP builds in src/assets/guides/ (693 KB), which are committed. Rebuild
# them with: python scripts/build_guides.py
src/assets/*.png

# trained model weights - too large for git, see backend/models/README.md
backend/models/*.h5
backend/models/*.pt
server/weights/

# training run outputs (canonical copies live in backend/models/)
notebooks/*.h5
notebooks/*.png

# downloaded datasets (~15 GB, see notebooks/train_xray_model_local.ipynb)
saans_data/
notebooks/*.json
backend/models/*.joblib

# MiniLM sentence encoder for the WHO assistant (~90 MB). Downloads itself on
# first use; the index it produces (who_index.npz) is small and is committed.
backend/models/embedding/
